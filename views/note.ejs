<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head'); %>
    <link rel="stylesheet" href="/styles/noteStyles.css" />
  </head>
  <body>
    <header><%- include('./partials/navBar.ejs') %></header>
    <% if (authenticated === false) { %> // TODO: Redesign this section to make
    it more user-friendly
    <h2>You are currently not logged in</h2>
    <h2>Please log in to continue</h2>
    <% } else { %>
    <div class="note-container">
      <h2>Your Note</h2>
      <form action="/note/<%= _id %>" method="post">
        <label for="title">Title:</label>
        <br />
        <input
          type="text"
          id="title"
          name="title"
          placeholder="Enter note title here..."
          value="<%= noteTitle %>"
        />
        <br />
        <label for="content">Content:</label>
        <br />
        <textarea
          name="content"
          id="content"
          placeholder="Write your note here..."
        ><%= content %></textarea>
        <button type="submit">Save</button>
      </form>
      <form class="pt-2" action="/note/delete/<%= _id %>" method="post">
        <button type="submit">Delete</button>
      </form>
    </div>
    <% } %>
  </body>
</html>
